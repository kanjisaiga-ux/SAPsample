projection;
strict ( 2 );
use draft;

define behavior for ZC_SA_AP_ROUTE alias Route {
  use create;
  use update;
  use delete;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}
